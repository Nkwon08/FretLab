Place static assets (images, icons, etc.) in this folder.

Expected files used by the app:
- pic.jpeg — Home page image

Folder is served statically by the local server, so references like `/assets/pic.jpeg` will work.

